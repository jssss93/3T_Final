<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mypage">


	<select id="mypageInfo" parameterType="hashmap"
		resultType="hashmap">
	<![CDATA[
		SELECT 
			MEMBER_NO,
			MEMBER_ID, 
			PASSWD, 
			NAME, 
			BIRTH, 
			ZIPCODE, 
			ADDR1, 
			ADDR2, 
			PHONE, 
			EMAIL, 
			REGDATE
		FROM 
			MEMBER 
		WHERE 
			MEMBER_ID = #{MEMBER_ID}
	]]>
	</select>
	

	   <select id="myBoardQna" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
           SELECT
           		QA_NO,
           		TITLE,
           		MEMBER_ID,
           		REGDATE
           	FROM 
           		QA
           	WHERE
           		MEMBER_ID = #{MEMBER_ID}	
        ]]>
    </select>
    
    	   <select id="myBoardReview" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
           SELECT
           		REVIEW_NO,
           		TITLE,
           		MEMBER_ID,
           		REGDATE
           	FROM 
           		REVIEW
           	WHERE
           		MEMBER_ID = #{MEMBER_ID}	
        ]]>
    </select>

	<select id="myCoupon" parameterType="hashmap"
		resultType="hashmap">
        <![CDATA[
           SELECT
           		COUPON_NO,
           		CONTENT
           	FROM 
           		COUPON
           	WHERE
           		MEMBER_ID = #{MEMBER_ID}	
        ]]>
	</select>
	
		<select id="myMessage" parameterType="hashmap"
		resultType="hashmap">
        <![CDATA[
           SELECT
           		MESSAGE_NO,
           		CONTENT,
           		SENDMEMBER
           	FROM 
           		MESSAGE
           	WHERE
           		TOMEMBER = #{TOMEMBER}	
        ]]>
	</select>
   		
		
	
    <insert id="insertCoupon" parameterType="hashmap" useGeneratedKeys="true" keyProperty="COUPON_NO">
    	<selectKey keyProperty="COUPON_NO" resultType="java.lang.Integer" order="BEFORE">
			SELECT COUPON_NO_SEQ.NEXTVAL FROM DUAL
		</selectKey>
    		<![CDATA[
		INSERT INTO COUPON 
			(COUPON_NO, CONTENT, MEMBER_ID)
		VALUES
			(#{COUPON_NO}, #{CONTENT}, #{MEMBER_ID})
		]]>
    </insert>
    
    <insert id="insertMessage" parameterType="hashmap" useGeneratedKeys="true" keyProperty="MESSAGE_NO">
    	<selectKey keyProperty="MESSAGE_NO" resultType="java.lang.Integer" order="BEFORE">
			SELECT M_MSG_NO_SEQ.NEXTVAL FROM DUAL
		</selectKey>
    		<![CDATA[
		INSERT INTO MESSAGE 
			(MESSAGE_NO, CONTENT, TOMEMBER, SENDMEMBER)
		VALUES
			(#{MESSAGE_NO}, #{CONTENT}, #{TOMEMBER}, #{SENDMEMBER})
		]]>
    </insert>

	<!-- 정보수정 -->
	<update id="updateMember" parameterType="hashmap">
		<![CDATA[
		UPDATE 
			MEMBER 
		SET 
			PASSWD = #{PASSWD}, ZIPCODE = #{ZIPCODE}, ADDR1 = #{ADDR1}, ADDR2 = #{ADDR2}, PHONE = #{PHONE}, EMAIL = #{EMAIL}
		WHERE 
			MEMBER_ID = #{MEMBER_ID}
		]]>


	
	</update>

</mapper>